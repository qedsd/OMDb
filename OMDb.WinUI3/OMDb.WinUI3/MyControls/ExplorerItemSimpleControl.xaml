<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<UserControl
    x:Class="OMDb.WinUI3.MyControls.ExplorerItemSimpleControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OMDb.WinUI3.MyControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:models="using:OMDb.WinUI3.Models"
    mc:Ignorable="d">

    <Grid>
        <TreeView x:Name="treeFolders" SelectionMode="Multiple" x:FieldModifier="public" Width="600">
            <TreeView.ItemTemplate>
                <DataTemplate x:DataType="models:ExplorerItem">
                    <TreeViewItem Height="20" ItemsSource="{x:Bind Children}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{x:Bind Name}" />
                        </StackPanel>
                    </TreeViewItem>
                </DataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>
