<Page
    x:Class="OMDb.WinUI3.Dialogs.AddEntryBatchDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:OMDb.WinUI3.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:OMDb.WinUI3.Models" 
    xmlns:myControls="using:OMDb.WinUI3.MyControls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid Width="1920" Height="720">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <StackPanel Orientation="Horizontal">
                <Button Content="全选" />
                <Button Margin="10,0,0,0" Content="全不选" />
                <Button Margin="10,0,0,0" Content="新增行" />
                <Button Margin="10,0,0,0" Content="删除行" />
                <Button
                    x:Name="btn_FolderPicker"
                    Margin="10,0,0,0"
                    Click="SelectFolders_Click"
                    Content="选择文件夹" >
                    <Button.Flyout>
                        <Flyout Placement="Bottom" x:Name="fly">
                            <Grid x:Name="flyGrid" Width="600" Height="500">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="400"></RowDefinition>
                                    <RowDefinition Height="380"></RowDefinition>
                                    <RowDefinition Height="55"></RowDefinition>
                                </Grid.RowDefinitions>
                                <Grid Grid.Row="0" Width="600">
                                    <myControls:ExplorerItemSimpleControl x:Name="ei" HorizontalAlignment="Left"></myControls:ExplorerItemSimpleControl>
                                </Grid>
                                <Grid Grid.Row="1" Margin="0,10,0,0">
                                    <ListView x:Name="eic" Width="600">
                                        <ListView.ItemTemplate>
                                            <DataTemplate x:DataType="x:String">
                                                <TextBlock Text="{x:Bind}" Margin="0,-5,0,-5"></TextBlock>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>

                                </Grid>
                                <Grid Grid.Row="2" Margin="10,0,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="确认" Click="ReturnFolder_Click"></Button>
                                        <Button Content="取消" Click="Cancel_Click" Margin="30,0,0,0"></Button>
                                        <Button Content="添加" Click="Add_Click" Margin="30,0,0,0"></Button>
                                    </StackPanel>
                                </Grid>
                                <Grid.RenderTransform>
                                    <CompositeTransform ScaleX="0.6" ScaleY="0.6" />
                                </Grid.RenderTransform>

                            </Grid>
                        </Flyout>
                    </Button.Flyout>
                </Button>
                <Button Margin="10,0,0,0" Content="获取信息" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <ListView
                Margin="0,20,0,0"
                ItemsSource="{x:Bind VM.EntryDetailCollection}"
                SelectionMode="Multiple">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:EntryDetail">
                        <Grid Height="100" HorizontalAlignment="Left">
                            <Image
                                x:Name="Image_CoverImg"
                                Width="60"
                                Height="80"
                                Source="{x:Bind FullCoverImgPath, Mode=OneWay}"
                                Stretch="Fill" />
                            <Button
                                Width="60"
                                Height="80"
                                VerticalAlignment="Center"
                                Background="Transparent" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
